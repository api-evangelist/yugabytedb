naftiko: 1.0.0-alpha2
info:
  label: YugabyteDB Anywhere v1 — Universe Management — Tablet server management
  description: 'YugabyteDB Anywhere v1 — Universe Management — Tablet server management. 1 operations. Lead operation: YugabyteDB
    Anywhere List All Tablet Servers Information. Self-contained Naftiko capability covering one Yugabytedb business surface.'
  tags:
  - Yugabytedb
  - Tablet server management
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    YUGABYTEDB_API_KEY: YUGABYTEDB_API_KEY
capability:
  consumes:
  - type: http
    namespace: anywhere-v1-universes-tablet-server-management
    baseUri: ''
    description: YugabyteDB Anywhere v1 — Universe Management — Tablet server management business capability. Self-contained,
      no shared references.
    resources:
    - name: api-v1-customers-cUUID-universes-uniUUID-tablet-servers
      path: /api/v1/customers/{cUUID}/universes/{uniUUID}/tablet-servers
      operations:
      - name: listtabletservers
        method: GET
        description: YugabyteDB Anywhere List All Tablet Servers Information
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: cUUID
          in: path
          type: string
          required: true
        - name: uniUUID
          in: path
          type: string
          required: true
    authentication:
      type: apikey
      key: X-AUTH-YW-API-TOKEN
      value: '{{env.YUGABYTEDB_API_KEY}}'
      placement: header
  exposes:
  - type: rest
    namespace: anywhere-v1-universes-tablet-server-management-rest
    port: 8080
    description: REST adapter for YugabyteDB Anywhere v1 — Universe Management — Tablet server management. One Spectral-compliant
      resource per consumed operation, prefixed with /v1.
    resources:
    - path: /v1/api/v1/customers/{cuuid}/universes/{uniuuid}/tablet-servers
      name: api-v1-customers-cuuid-universes-uniuuid-tablet-servers
      description: REST surface for api-v1-customers-cUUID-universes-uniUUID-tablet-servers.
      operations:
      - method: GET
        name: listtabletservers
        description: YugabyteDB Anywhere List All Tablet Servers Information
        call: anywhere-v1-universes-tablet-server-management.listtabletservers
        with:
          cUUID: rest.cUUID
          uniUUID: rest.uniUUID
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: anywhere-v1-universes-tablet-server-management-mcp
    port: 9090
    transport: http
    description: MCP adapter for YugabyteDB Anywhere v1 — Universe Management — Tablet server management. One tool per consumed
      operation, routed inline through this capability's consumes block.
    tools:
    - name: yugabytedb-anywhere-list-all-tablet
      description: YugabyteDB Anywhere List All Tablet Servers Information
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: anywhere-v1-universes-tablet-server-management.listtabletservers
      with:
        cUUID: tools.cUUID
        uniUUID: tools.uniUUID
      outputParameters:
      - type: object
        mapping: $.
